{% extends 'base.html.twig' %}

{% block title %}Departement index{% endblock %}

{% block body %}
    <h1>Departement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for d in departement %}
            <tr>
                <td>{{ d.id }}</td>
                <td>{{ d.description }}</td>
               
                <td>
                    <a href="{{ path('departement_edit', {id: d.id}) }}">Modifier</a>
                </td>
                <td>
                    <a href="{{ path('add') }}">Ajouter</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   
{% endblock %}
